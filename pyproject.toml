[project]
name = "agent-shared-memory-demo"
version = "0.1.0"
description = "Minimal demo for agent shared memory with enterprise private data."
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]

